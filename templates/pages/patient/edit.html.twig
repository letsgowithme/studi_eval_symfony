{% extends 'base.html.twig' %}
{% block title %} Éditer le patient {% endblock %}
{% block body %}
<div class="container">

<h1 class="mt-4">Modifier le patient</h1>

{{ form_start(form) }}
<div class="form-group">
{{ form_label(form.name) }}
{{ form_widget(form.name) }}
    <div class="form-error">
    {{ form_errors(form.name) }}
    </div>
</div>

{{ form_end(form) }}

</div>
{% endblock %}



